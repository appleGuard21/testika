spring:
  datasource:
    password:
    username:
    url: jdbc:postgresql://localhost:5432/my_db
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop